{
  "name": "pi-tiny-tools",
  "version": "0.1.0",
  "description": "Compact tool and extension-message rows for Pi",
  "type": "module",
  "keywords": ["pi-package", "pi", "pi-coding-agent", "tool-trace"],
  "license": "MIT",
  "files": ["README.md", "src"],
  "pi": {
    "extensions": ["./src/index.ts"]
  },
  "scripts": {
    "test": "node --experimental-strip-types --test 'test/**/*.test.ts'",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm test"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.84.4",
    "@earendil-works/pi-tui": "^0.84.4",
    "@types/node": "^22.19.19",
    "typescript": "^5.9.3"
  }
}
